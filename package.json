{
  "name": "array-iterator",
  "description": "array iterator fallback until node has them",
  "keywords": ["array", "iterator", "generator"],
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git://github.com/jb55/array-iterator.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "mocha --harmony --reporter spec"
  },
  "dependencies": {},
  "devDependencies": {
    "mocha": "*"
  }
}
